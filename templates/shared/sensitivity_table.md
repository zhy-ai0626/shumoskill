# 验证、灵敏度与稳健性报告模板

> Stage 6 快速参考。按题目风险选择表格和图，不需要全部填写。详细流程见 `../../references/stage_06_robustness.md`。

## 0. 核心主张与风险

| 核心主张 | 可能失效的原因 | 影响的子问题/结论 | 验证优先级 |
|---|---|---|---|
| `<待填>` | `<参数误差 / 数据漂移 / 随机性 / 结构假设 / 离散边界>` | `<待填>` | `<高/中/低及理由>` |

## 1. 验证设计

| 风险 | 方法 | 选择理由 | 数据/参数域依据 | 样本、切分或种子 | 预先定义的判断标准 |
|---|---|---|---|---|---|
| `<待填>` | `<OAT / 联合抽样 / Sobol / 重采样 / 时序留出 / 情景分析 / 边界扫描>` | `<为何与风险匹配>` | `<附件、置信区间、物理边界或假设场景>` | `<待填>` | `<误差、可行性、决策变化等>` |

说明：

- OAT 可以用于有依据的局部单参数问题；交互风险明显时再使用联合设计。
- LHS、Sobol、Morris 的适用问题与成本不同，按风险和样本预算选择，不做等级排序。
- 参数范围和样本预算必须有依据；没有数据依据时标记为假设场景。

## 2. 定量结果

| 测试域/数据切分 | 方法与运行标识 | 关键性能指标 | 决策变化 | 可行性/失败样本 | 结论范围 |
|---|---|---|---|---|---|
| `<待填>` | `<脚本、版本、seed>` | `<点估计 + 区间 + 单位>` | `<绝对/相对差或排序变化>` | `<数量、位置、原因>` | `<只描述已测试范围>` |

如果使用随机抽样，补充收敛检查：

| 初始样本/重复数 | 追加后样本/重复数 | 关注统计量的变化 | 是否足以支持当前结论 | 证据路径 |
|---|---|---|---|---|
| `<待填>` | `<待填>` | `<待填>` | `<是/否及理由>` | `<results/...>` |

## 3. 参数或场景贡献（可选）

只在方法支持归因时填写；相关系数、Tornado 与 Sobol 指数不是同一个量。

| 参数/场景 | 贡献指标及定义 | 数值与区间 | 交互或依赖 | 解读限制 |
|---|---|---|---|---|
| `<待填>` | `<相关、弹性、S1/ST、效应差等>` | `<待填>` | `<待填>` | `<不能推出什么>` |

## 4. 失败边界与未测试区域

| 条件 | 观察状态 | 现象 | 受影响结论 | 行动 |
|---|---|---|---|---|
| `<待填>` | `<observed / not_observed / untested>` | `<不可行、误差超限、方案切换或未发现>` | `<待填>` | `<回退、限制措辞、补测或记录>` |

没有观察到临界点时，写“在已测试域内未观察到失败边界”；不要为了填表虚构阈值。

## 5. 图表选择

根据证据需求选择最少且足够的图：

- 参数—结果关系图：非线性、交互和边界；
- 区间图/箱线图：场景、重采样或随机种子比较；
- 时序/分组误差图：漂移与外推；
- 可行域或方案切换图：离散决策风险；
- Tornado/Sobol 图：只有完成对应贡献度分析时使用。

| 图 | 回答的问题 | 数据与方法 | 正文结论 |
|---|---|---|---|
| `<path>` | `<待填>` | `<待填>` | `<待填>` |

实现起点见 `code_starter/simulation.py`。示例代码需要按题型、相关结构和指标修改后才能使用。

## 6. L2 回检

| 回检对象 | 当前证据 | 是否被挑战 | 后续动作 |
|---|---|---|---|
| Stage 3 模型选择 | `<待填>` | `<是/否/未验证>` | `<保留/补测/回退>` |
| Stage 4 假设 | `<待填>` | `<是/否/未验证>` | `<保留/放宽/披露>` |
| Stage 5 结果复用 | `<待填>` | `<是/否/未验证>` | `<保留/重算/限制范围>` |

## 自检

- 方法是否针对核心风险，而不是为了使用某个算法；
- 参数域、切分、样本预算、种子和判断标准是否可追溯；
- 是否同时报告结果变化、决策变化和适用的可行性检查；
- 是否区分观察到的边界、未观察到的边界与未测试区域；
- 是否避免把测试域内的结果外推为“模型普遍稳健”。
