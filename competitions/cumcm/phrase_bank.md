# 中文学术句式库 (phrase_bank)

> 按章节分类的中文建模写作句式。它们只提供表达骨架；每个数据、比较、因果和“创新”声明都必须能回指题面、代码、结果或可靠来源，不能为了文风机械套用。

---

## 1. 摘要 (Abstract)

### 1.1 开篇背景
- 随着<领域/问题>的不断发展,<挑战>已成为亟需解决的关键问题。
- 在<场景>背景下,如何<目标>是一个具有重要现实意义的问题。
- 针对<问题领域>中<具体问题>的求解需求,本文进行了如下研究。

### 1.2 总体思路
- 本文在综合考虑<因素 1>、<因素 2>和<因素 3>的基础上,建立了<模型族>模型。
- 本文通过<方法 1>对<对象>进行<操作>,进而采用<方法 2>实现<目标>。
- 围绕<核心问题>,本文从<维度 A>和<维度 B>两个角度展开建模。

### 1.3 子问题方法
- **针对问题一**,本文<动作>...通过<求解方法>得到<定量结果>。
- **对于问题二**,本文在问题一基础上,引入<新因素>,建立<升级模型>...
- **进一步**,针对问题三,本文将问题一中得到的<结果>作为输入,...

### 1.4 灵敏度与稳健性
- 根据<数据/业务依据>设定<参数> ∈ <范围>并进行灵敏度分析后，<指标>偏差为 X%；因此结论在该范围和当前场景内保持<限定描述>。
- 在<有依据的参数范围>内进行多变量联合扰动后，模型输出的<指标>分布为<结果>；超出<边界>时需重新验证。

### 1.5 创新与推广
- 相对<已定义基线>,本文实际加入<机制/约束>; <对照实验或分析>显示其对<指标>的影响为<结果>。
- 在<共同机制成立>的场景中，可替换<数据/参数/约束>后重新验证；当前结论不直接外推到未测试场景。

---

## 2. 问题重述 / 问题分析

### 2.1 重述
- 本题要求在<约束条件>下,<目标动作>,具体包括以下三个子问题:
- 综合分析题目所给条件,本题的核心在于<核心问题>。

### 2.2 子问题分析
- 问题一本质上是一个<问题类型,如多目标优化/参数估计/分类>问题。
- 该问题需在<约束>下,寻找<决策变量>使<目标函数>取得最优。
- 由于<难点>,直接求解较为困难,需通过<策略>转化或近似处理。

---

## 3. 模型假设 (Assumption)

### 3.1 假设引入
- 为简化模型并保证可解性,本文做出如下假设。
- 在不影响主要结论的前提下,作以下假设。

### 3.2 假设支撑
- **假设 i**: <内容>。**依据**: 文献 [N] 在类似场景下采用相同假设;附件 X 数据显示<观察>。
- **假设 i**: <内容>。**理由**: 从<物理意义/经济意义/工程惯例>考虑,该假设合理。

---

## 4. 模型建立 (Model Building)

### 4.1 决策变量
- 设 x_i 为<含义>(单位: <单位>),i = 1, 2, ..., n。
- 引入决策变量 x = (x_1, ..., x_n)^T,其中 x_i 表示<含义>。

### 4.2 目标函数
- 综合考虑<因素>,目标函数定义为:
- 由于<目标>,本文以最小化<指标>作为优化目标。

### 4.3 约束条件
- 由<物理/经济/逻辑>约束,有:
- 此外,决策变量需满足非负约束 / 整数约束 / 0-1 约束。

### 4.4 模型组装
- 综上,问题 i 的数学规划模型为:
- 该模型为一个<问题类型, e.g., 混合整数二次规划>问题。

---

## 5. 模型求解 (Solution)

### 5.1 求解策略选择
- 由于本模型为<难度类型>,直接求解时间复杂度过高,本文采用<算法>求解。
- 考虑到<约束特点>,本文设计基于<核心思想>的<算法>。

### 5.2 算法步骤
- 算法的具体步骤如下: **Step 1**: <动作>; **Step 2**: <动作>; ...
- 算法流程图见图 N。

### 5.3 求解结果
- 通过 Python 调用 <求解器/库> 求解，经 <时间> 后返回<状态>; 在<最优性界/收敛条件/近似条件>下得到解 x = (...)。
- 在相同数据、预算与指标下，该方案的<指标>为 X，相对<已定义基线>变化 Y%；比较限制为<限制>。

---

## 6. 结果分析 (Result Analysis)

### 6.1 数值结果
- 求解得到 <指标> = X (单位),<指标> = Y。
- 表 N 列出了在不同<情境>下的<指标>对比。

### 6.2 物理意义解释
- 该结果意味着<现实含义>。
- 这与<已引用的现实数据或已有研究>在<具体方面>一致，为<有限结论>提供旁证，但不能替代独立验证。

### 6.3 与基线对比
- 相比传统<方法>,本模型在<指标>上提升 X%,在<指标>上降低 Y%。
- 表 N 给出了三种方法的对比结果。

---

## 7. 灵敏度 / 稳健性

### 7.1 引言
- 为评估模型对参数变化的敏感程度,本文进行了灵敏度分析。
- 采用拉丁超立方抽样 (LHS) 对参数 (α, β, γ) 联合扰动。

### 7.2 结果报告
- 当 α ∈ [a, b] 变化时,<指标>变化范围为 [c, d],变动幅度 X%。
- 表 N 与图 N 直观展示了<指标>对各参数的敏感性。

### 7.3 稳健性结论
- 综合分析,模型在 <参数范围> 内表现稳健,可应用于实际场景。

---

## 8. 模型评价 (Evaluation)

### 8.1 优点
- **与题目约束对应**: <逐项映射证据>。
- **计算表现**: 在<硬件、规模和停止条件>下耗时<数据>。
- **可解释性范围**: <哪些变量或机制可解释，以及不能解释什么>。

### 8.2 缺点
- **<具体方法>未纳入**: 若改用 <替代方法>,在 <指标> 上预计提升 X%,但 <代价>。
- **数据假设可能过强**: 当前假设 <Y>,实际场景中 <Z>。

### 8.3 推广
- 本模型可推广至 <场景 1>: <适配方式>。
- 在 <场景 2> 中应用本模型,需将 <参数> 替换为 <新参数>。

---

## 9. 连接词 / 过渡词 (使用频率)

### 9.1 因果
- 由于... 因此... / 鉴于... 故... / 基于... 可得...

### 9.2 递进
- 进一步地... / 在此基础上... / 不仅如此...

### 9.3 转折
- 然而... / 但需注意... / 值得指出的是...

### 9.4 总结
- 综上所述... / 综合分析... / 基于上述讨论...

### 9.5 类比
- 类似地... / 同理可得... / 与之相对...

---

## 10. 危险句式 (避免使用)

LLM 写作的"机器味"信号词,**主动避免**:

- ❌ "本模型完美地..." (太绝对)
- ❌ "充分体现了..." (空)
- ❌ "深入探讨了..." (套话)
- ❌ "丰富的研究意义" (口水话)
- ❌ "为...提供了重要参考" (无信息)
- ❌ "具有广泛的应用前景" (空泛)

**替换**:
- ✅ "本模型在 X 指标上达到 Y" (具体)
- ✅ "通过 X,本文实现 Y" (有动作有结果)
- ✅ "在 X 数据集上,本方法误差 Y%,优于 Z" (可验证)

---

## 11. LaTeX 公式术语 (常忘的)

| 中文 | LaTeX |
|------|-------|
| 服从分布 | $X \sim N(\mu, \sigma^2)$ |
| 最大化 | $\max_{x \in \mathcal{X}} f(x)$ |
| 集合 | $\mathcal{X}, \mathcal{Y}$ (花体) |
| 上确界 | $\sup$ / $\inf$ |
| 趋向于 | $\to$ / $\rightarrow$ |
| 期望 | $\mathbb{E}[X]$ / $E(X)$ |
| 概率 | $\mathbb{P}(A)$ / $P(A)$ |
| 实数 | $\mathbb{R}$ |
| 矩阵转置 | $A^T$ / $A^\top$ |
| 范数 | $\|x\|_2$ / $\|x\|_\infty$ |

---

## 用法

- stage 8 写每节时可从对应章节选择适用句式，但应按题目重写，避免重复和模板腔
- L1 critic 用下面“§12 信息功能与证据链检查”核对事实完整性，不按词语命中数打分
- 危险句式 (§10) 只作为人工复核信号；若后文已有充分限定和证据，不机械扣分

---

## §12 L1 信息功能与证据链检查

检查重点是每个声明能否被核验，而不是是否出现某个固定短语。

### 摘要信息功能

| 功能 | 检查问题 |
|----|----------|
| 问题与任务 | 是否准确对应题面，且没有遗漏实际作答的子问题？ |
| 方法 | 模型名称是否与正文定义和代码实现一致？ |
| 结果 | 数值是否带指标、单位和场景，并能回指结果文件或正文表格？ |
| 验证与边界 | 若结论依赖不确定参数，是否说明验证方法、范围依据和失效条件？ |
| 贡献或适用范围 | 若声称改进、创新或推广，是否给出基线、对照证据和重新验证条件？ |

这些功能可以合并在同一段，也可以按子问题拆开；并非每篇论文都需要“创新”或“推广”段。

### 危险句式复核

若命中 phrase_bank §10 的绝对化或空泛句式，检查是否能改写为“对象 + 条件 + 指标 + 证据”。单纯词语命中不设分数上限。

### 章节证据链

- §5 模型建立: 变量、目标、约束和求解方法是否覆盖题面任务，并能从结果回溯到输入与假设？
- §6 验证与稳健性: 验证方法是否针对主要风险，范围是否有数据或业务依据，结论是否带边界？
- §7 评价: 每条优点和缺点是否有证据与影响说明；推广只在有共同机制时写，并注明适配代价。

L1 critic 的 evidence 应记录原句、行号和对应证据路径；仅 grep 关键词不足以证明内容质量。

---

## 与 anti_patterns 的双向引用 (P3-3)

- 危险句式 (§10) ↔ `anti_patterns.md` A4 (关键词低质量) / H1 (套话式自我批判) / H3 (推广空泛)
- §12 信息功能检查 ↔ `anti_patterns.md` A1 (摘要无定量) / A2 (摘要组织混乱) / A5 (摘要过短/长)
